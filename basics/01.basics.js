const accountId = 1200;
let accountName = "Pradeep Sahu";
data = "nodata";
var duplicate;
console.table([accountId, accountName, data, duplicate]);

let age = 12;
let name = "Pradeep Sahu";
let isLoggedIn = false;
let empty = null;

//number = 2^52
//bigint
//string
//boolean
//null => standalone value (type) - representation of empty value
//undefined => value not assigned
//symbol => to find the uniqueness in react. (component) - unique

//Object

//null is of type of an object
// undefined is of type undefined

console.table([age, name, isLoggedIn, empty]);

console.log(typeof undefined);
